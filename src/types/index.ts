type ContactList = {
  title: string;
  url: string;
  icon: React.ReactNode;
};

type NavLinkList = {
  href: string;
  title: string;
  isActive: boolean;
};

export type { ContactList, NavLinkList };
